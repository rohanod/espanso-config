# Terminal Snippets

## Included snippets

- `!x-docker-log`: Clear Docker logs for a specified container.
- `!inst-docker`: Install Docker and add the current user to the Docker group.

## Install Command

`espanso install term-snippets --git https://github.com/rohanod/espanso-config --external`